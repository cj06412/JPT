# JPT v1.5 Implementation Plan — 灵魂注入

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 v1.0「礼物可送」基础上做「灵魂注入」—— 让 JPT 真的活起来：对话框完整（tool_use 卷轴卡片 / 点外面关 / 斜杠命令 / 设置改人格真落地）、动画丰富（4 帧走路 / 4 表情立绘 / idle 呼吸 / 挂壁摇摆）、有生命感（音效 / 主动陪伴）、微信人格 skill 脚手架，外加多屏 / 任务栏 autohide 收尾。

**Architecture:** 沿用 v1 的双进程双窗 + IPC。新增的事件（tool_use / tool_result / 主动消息）统一走 main → renderer IPC，和现有 `dialog:stream-token` 等同一套机制。动画全部 React + CSS / 帧切换驱动（不引入 PixiJS —— v1 已证明 `<img image-rendering:pixelated>` + CSS transform 够用，YAGNI）。资源依赖项（4 帧 sprite / 4 表情立绘 / 音效）沿用 v1 的「占位 + 脚手架先行，真资源后补，换文件即生效」策略。

**Tech Stack:** 复用 v1 全栈（Electron 33 / Vite 5 / React 19 / TS strict / Vitest 1 / `claude` CLI）。新增：HTML5 `Audio` API（音效，零依赖）、`screen` 模块的 `display-*` 事件（多屏）。**不新增 npm 依赖。**

**参考：** spec `docs/superpowers/specs/2026-05-10-JPT-design.md` §2.2（v1.5 范围）、§3.1（idle/cling 动画细节）、§3.3（多屏/任务栏）、§6.4（tool_use 视觉）。v1 acceptance `docs/v1-acceptance.md` 的「Known v1.5 follow-ups」9 条（其中 #2 角色冻结已在 v1 commit aea5bc5 完成，本 plan 不含）。

**前置条件：**
- v1.0 已完成 + push（39+ commits 在 origin/main）
- 当前代码结构（plan 据此写）：
  - `electron/agent/claude.ts` `handleEvent` 处理 `system+init` / `assistant` / `result`，**不处理** `tool_use` / `tool_result`
  - `electron/agent/session.ts` `AgentSessionCallbacks` = onText / onError / onSessionReady / onTurnComplete / onProcessExit
  - `electron/agent/workdir.ts` `ensureWorkdir(basePath)` 写占位 `CLAUDE.md`（不覆盖已存在）
  - `electron/ipc.ts` `registerIpcHandlers(windows, session, config, history, callbacks)` + `toggleDialog(windows)` + `openSettingsWindow(state)`
  - `electron/config-store.ts` ConfigStore；`src/shared/config.ts` `ConfigSnapshot` 已含 `personaDoc` 字段
  - `src/dialog/App.tsx` `Msg = { role:'user'|'assistant'|'error'; text }`，渲染用 `<Markdown>` / `<span>`
  - `src/character/App.tsx` rAF loop + drag + dialog-freeze（`dialogOpenRef`），渲染 `<div bg:red><img jpt-walk.png></div>`
  - 角色 spawn `claude` 时 `--setting-sources project,local` —— **project 级 = workdir/.claude/，所以 workdir/.claude/skills/ 里的 skill 会被自动发现**（v1.5 微信 skill 靠这个生效）
- 资源占位脚本 `scripts/gen-placeholder-art.ps1` 仍在

---

## 文件结构（v1.5 终态，只列新增 / 改动）

```
JPT/
├── electron/
│   ├── agent/
│   │   ├── claude.ts            (modify — handleEvent 处理 tool_use / tool_result)
│   │   ├── session.ts           (modify — AgentSessionCallbacks 加 onToolUse / onToolResult)
│   │   └── workdir.ts           (modify — 落 personaDoc 覆盖逻辑 + 建 .claude/skills 脚手架)
│   ├── ipc.ts                   (modify — tool IPC / dialog blur 关闭 / 斜杠命令 / 主动消息 / persona 落地)
│   ├── main.ts                  (modify — dialog blur 监听 + 主动陪伴定时器装配)
│   ├── proactive.ts             (新 — 主动陪伴定时器 + 文案表，纯逻辑可测)
│   └── window-manager.ts        (modify — 多屏活跃屏 + 任务栏 autohide 几何)
├── src/
│   ├── character/
│   │   ├── App.tsx              (modify — 4 帧走路 / idle 呼吸 / cling 摇摆 / 表情事件桥接)
│   │   ├── sprite-sheet.ts      (新 — 4 帧走路帧选择纯函数 + 测试)
│   │   └── state-machine.ts     (无改动 — 帧动画在渲染层算，不进状态机)
│   ├── dialog/
│   │   ├── App.tsx              (modify — tool Msg / 斜杠命令 / 主动消息渲染)
│   │   ├── ToolUseCard.tsx      (新 — SDV 卷轴小卡)
│   │   ├── PortraitPanel.tsx    (modify — 按 expression prop 切换 4 张立绘)
│   │   └── slash.ts             (新 — 斜杠命令解析纯函数 + 测试)
│   └── shared/
│       ├── messages.ts          (modify — AgentRole 加 toolUse/toolResult；已有这两个值，确认)
│       └── proactive-messages.ts (新 — 主动陪伴文案常量，character + dialog 共用类型)
├── assets/
│   ├── sprites/
│   │   ├── jpt-walk.png         (v1 单帧 — v1.5 起改成 4 帧横向 sprite sheet，96×128 → 384×128)
│   │   ├── jpt-portrait.png     (默认表情 — 保留)
│   │   ├── jpt-portrait-smile.png    (新 — 占位)
│   │   ├── jpt-portrait-think.png    (新 — 占位)
│   │   └── jpt-portrait-confused.png (新 — 占位)
│   └── sounds/                  (新目录)
│       ├── complete.mp3         (占位静音，真音效后补)
│       ├── click.mp3            (占位静音)
│       └── type.mp3             (占位静音)
├── scripts/
│   └── gen-placeholder-art.ps1  (modify — 也生成 4 帧 sheet + 3 张表情占位 + 静音 mp3)
├── tests/
│   ├── sprite-sheet.test.ts     (新)
│   ├── slash.test.ts            (新)
│   └── proactive.test.ts        (新)
└── docs/
    ├── superpowers/plans/2026-05-15-JPT-v1.5.md  (本文档)
    ├── wechat-persona-guide.md  (新 — 微信聊天记录 → SKILL.md 的手工抽取指南)
    └── v1.5-acceptance.md       (新 — Stage 5 末产出)
```

---

## Windows 注意事项（沿用 v1，新增项）

| 坑 | 出在哪 | 应对 |
|---|---|---|
| 透明窗 GPU / 节流 | 全程 | v1 已 `app.disableHardwareAcceleration()` + `backgroundThrottling:false` + 3 个 app switch；v1.5 不再处理 |
| preload 必须 CJS | 全程 | v1 已用静态 `electron/preload.cjs.js`；v1.5 不动 |
| 持久人格隔离 | 全程 | v1 已 `cwd=workdir` + `--setting-sources project,local` + `--strict-mcp-config`；微信 skill 放 workdir/.claude/skills/ 正好被 project 级捡到 |
| `Audio` 在 sandbox renderer | Stage 3 | renderer 用 HTML5 `Audio`，资源走 Vite `?url` import（和 zpix 同款）；contextIsolation 不影响 Audio |
| 多屏 `display-added/removed` | Stage 5 | `screen` 模块只在 main 进程可用；renderer 不直接碰 |
| 任务栏 autohide workArea = 全屏 | Stage 5 | Win11 autohide 时 `workArea` == `bounds`；检测 `bounds.height - workArea.height < 4` 判定 autohide，角色 y 减一个安全边距 |

---

## Stage 1：对话框完整化（tool_use 卡片 / 点外关 / 斜杠命令 / 设置改人格真落地）

### Task 1: AgentSession 加 tool 回调 + claude.ts 解析 tool_use / tool_result

**Files:**
- Modify: `electron/agent/session.ts`
- Modify: `electron/agent/claude.ts`

- [ ] **Step 1.1: 扩展 AgentSessionCallbacks**

打开 `electron/agent/session.ts`。把 `AgentSessionCallbacks` 接口替换为：

```ts
export interface AgentSessionCallbacks {
  onText: (chunk: string) => void
  onError: (msg: string) => void
  onSessionReady: () => void
  onTurnComplete: () => void
  onProcessExit: () => void
  onToolUse: (tool: string, summary: string) => void
  onToolResult: (summary: string, isError: boolean) => void
}
```

（其余接口不动。`Partial<AgentSessionCallbacks>` 在 ClaudeSession 里用，新增两个回调自动是可选的。）

- [ ] **Step 1.2: claude.ts handleEvent 处理 tool_use（assistant 块内）**

打开 `electron/agent/claude.ts`，找到 `handleEvent` 里 `if (type === 'assistant')` 那段。它当前只挑 `block.type === 'text'`。替换整个 `if (type === 'assistant') { ... }` 块为：

```ts
    if (type === 'assistant') {
      const message = ev.message as { content?: Array<Record<string, unknown>> } | undefined
      const blocks = message?.content ?? []
      for (const block of blocks) {
        if (block.type === 'text' && typeof block.text === 'string') {
          this.currentResponseText += block.text
          this.cb.onText?.(block.text)
        } else if (block.type === 'tool_use' && typeof block.name === 'string') {
          // Summarize the tool input compactly for the SDV scroll card.
          const input = block.input as Record<string, unknown> | undefined
          const summary = summarizeToolInput(block.name, input)
          this.cb.onToolUse?.(block.name, summary)
        }
      }
      return
    }
```

- [ ] **Step 1.3: claude.ts handleEvent 处理 tool_result（user 消息内）**

在 `handleEvent` 里，`if (type === 'result')` 块之前插入：

```ts
    if (type === 'user') {
      // Claude streams tool results back as a user-role message whose content
      // is an array of tool_result blocks.
      const message = ev.message as { content?: Array<Record<string, unknown>> } | undefined
      const blocks = message?.content ?? []
      for (const block of blocks) {
        if (block.type === 'tool_result') {
          const isError = block.is_error === true
          const raw = block.content
          let summary: string
          if (typeof raw === 'string') {
            summary = raw
          } else if (Array.isArray(raw)) {
            summary = raw
              .map((b) => (b && typeof b === 'object' && typeof (b as Record<string, unknown>).text === 'string'
                ? (b as Record<string, unknown>).text as string
                : ''))
              .join('')
          } else {
            summary = ''
          }
          // Trim to a card-sized blurb.
          const trimmed = summary.trim().slice(0, 280)
          this.cb.onToolResult?.(trimmed, isError)
        }
      }
      return
    }
```

- [ ] **Step 1.4: 加 summarizeToolInput 纯函数**

在 `electron/agent/claude.ts` 文件末尾（class 之后）追加：

```ts
/**
 * Compact one-line description of a tool call for the dialog scroll card.
 * WebSearch → the query; WebFetch → the URL; TodoWrite → "更新进度";
 * anything else → the tool name.
 */
function summarizeToolInput(tool: string, input: Record<string, unknown> | undefined): string {
  if (!input) return tool
  if (tool === 'WebSearch' && typeof input.query === 'string') return `搜「${input.query}」`
  if (tool === 'WebFetch' && typeof input.url === 'string') return `打开 ${input.url}`
  if (tool === 'TodoWrite') return '更新进度'
  return tool
}
```

- [ ] **Step 1.5: tsc + 既有测试不回归**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc exit 0；37 测试仍全过（本 task 没加测试，纯结构扩展，不能回归）。

- [ ] **Step 1.6: Commit**

```bash
git add electron/agent/session.ts electron/agent/claude.ts
git commit -m "feat(agent): parse tool_use / tool_result, add onToolUse/onToolResult callbacks"
```

---

### Task 2: tool IPC 通道 + ToolUseCard 组件 + dialog 渲染

**Files:**
- Modify: `electron/ipc.ts`
- Create: `src/dialog/ToolUseCard.tsx`
- Modify: `src/dialog/App.tsx`

- [ ] **Step 2.1: ipc.ts 把 tool 回调桥到 dialog renderer**

打开 `electron/ipc.ts`，找到 `session.setCallbacks({ ... })` 那段（v1 里有 onSessionReady / onText / onTurnComplete / onError / onProcessExit）。在 `onError` 之后、`onProcessExit` 之前插入两个回调：

```ts
    onToolUse: (tool, summary) => {
      windows.dialog.webContents.send('dialog:tool-use', { tool, summary })
      history.append({ ts: Date.now(), role: 'toolUse', tool, summary })
    },
    onToolResult: (summary, isError) => {
      windows.dialog.webContents.send('dialog:tool-result', { summary, isError })
      history.append({ ts: Date.now(), role: 'toolResult', summary, isError })
    },
```

（`HistoryEntry` 在 `electron/history-store.ts` 里已支持 `toolUse` / `toolResult` role —— v0 plan Task 15 定义时就含。无需改 history-store。）

- [ ] **Step 2.2: ToolUseCard 组件**

Create `src/dialog/ToolUseCard.tsx`:

```tsx
import { theme } from '@shared/theme'

export interface ToolUseCardProps {
  tool: string
  summary: string
  result?: string
  isError?: boolean
}

/**
 * SDV-flavored scroll card for a tool call (spec §6.4). TodoWrite collapses
 * to a faint "▸ 思考中…" line; web tools show the action + (when the result
 * has arrived) a trimmed blurb.
 */
export function ToolUseCard({ tool, summary, result, isError }: ToolUseCardProps) {
  if (tool === 'TodoWrite') {
    return <div style={{ color: theme.paperInkFaded, fontStyle: 'italic', margin: '4px 0' }}>▸ 思考中…</div>
  }
  const icon = tool === 'WebSearch' ? '🔍' : tool === 'WebFetch' ? '🌐' : '🔧'
  return (
    <div
      style={{
        margin: '6px 0',
        border: `2px solid ${theme.woodOutline}`,
        background: '#e8c896',
        padding: '6px 10px',
        fontSize: 13,
        color: isError ? theme.error : theme.paperInk,
        boxShadow: '2px 2px 0 rgba(0,0,0,0.25)',
      }}
    >
      <div style={{ fontWeight: 'bold' }}>{icon} {summary}</div>
      {result && (
        <div style={{ marginTop: 4, opacity: 0.85, whiteSpace: 'pre-wrap' }}>
          {isError ? '出错了：' : ''}{result}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2.3: dialog App.tsx — Msg 联合类型 + tool 事件 + 渲染**

打开 `src/dialog/App.tsx`。

(a) 替换 `interface Msg { ... }` 为联合类型：

```tsx
type Msg =
  | { role: 'user' | 'assistant' | 'error'; text: string }
  | { role: 'tool'; tool: string; summary: string; result?: string; isError?: boolean }
```

(b) 顶部 import 加：

```tsx
import { ToolUseCard } from './ToolUseCard'
```

(c) 在现有 `useEffect` 里（注册 `dialog:stream-token` / `dialog:turn-complete` / `dialog:error` 的那个），在 `offError` 注册之后、`return () => {...}` 之前加两个监听：

```tsx
    const offToolUse = window.jpt.on('dialog:tool-use', (...args: unknown[]) => {
      const { tool, summary } = args[0] as { tool: string; summary: string }
      setMsgs((prev) => [...prev, { role: 'tool', tool, summary }])
    })
    const offToolResult = window.jpt.on('dialog:tool-result', (...args: unknown[]) => {
      const { summary, isError } = args[0] as { summary: string; isError: boolean }
      setMsgs((prev) => {
        // Attach the result to the most recent tool card lacking one.
        for (let i = prev.length - 1; i >= 0; i--) {
          const m = prev[i]
          if (m.role === 'tool' && m.result === undefined) {
            const next = [...prev]
            next[i] = { ...m, result: summary, isError }
            return next
          }
        }
        return [...prev, { role: 'tool', tool: '🔧', summary: '', result: summary, isError }]
      })
    })
```

更新 cleanup 的 `return () => { offToken(); offComplete(); offError() }` 为：

```tsx
    return () => { offToken(); offComplete(); offError(); offToolUse(); offToolResult() }
```

(d) 在 `msgs.map(...)` 渲染里，处理 `tool` role。找到现有：

```tsx
          {msgs.map((m, i) => (
            <div key={i} style={{ marginBottom: 8, color: m.role === 'error' ? theme.error : theme.paperInk }}>
              <strong>{m.role === 'user' ? '我：' : m.role === 'error' ? '错误：' : 'JPT：'}</strong>{' '}
              {m.role === 'assistant' ? <Markdown text={m.text} /> : <span>{m.text}</span>}
            </div>
          ))}
```

替换为：

```tsx
          {msgs.map((m, i) => {
            if (m.role === 'tool') {
              return <ToolUseCard key={i} tool={m.tool} summary={m.summary} result={m.result} isError={m.isError} />
            }
            return (
              <div key={i} style={{ marginBottom: 8, color: m.role === 'error' ? theme.error : theme.paperInk }}>
                <strong>{m.role === 'user' ? '我：' : m.role === 'error' ? '错误：' : 'JPT：'}</strong>{' '}
                {m.role === 'assistant' ? <Markdown text={m.text} /> : <span>{m.text}</span>}
              </div>
            )
          })}
```

- [ ] **Step 2.4: tsc + 测试 + 手动 smoke**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc 0；37 过。手动：`npm run dev`，点红方块开对话框，问「帮我查一下今天上海天气」（会触发 WebSearch）—— 应看到一张米色卷轴卡 `🔍 搜「上海天气」`，结果回来后卡片下方追加 trimmed 结果，然后 JPT 正文继续。

- [ ] **Step 2.5: Commit**

```bash
git add electron/ipc.ts src/dialog/ToolUseCard.tsx src/dialog/App.tsx
git commit -m "feat(dialog): SDV scroll card for tool_use / tool_result events"
```

---

### Task 3: 点对话框外面关闭 + Esc 仍可关

**Files:**
- Modify: `electron/main.ts`
- Modify: `electron/ipc.ts`

spec §3.2：「点击对话框外（屏幕其它区域）→ 关闭对话框」。v1 只做了 Esc。dialog 窗 `focusable` 默认 true，失焦即视为点了外面。

- [ ] **Step 3.1: dialog 失焦时关闭**

打开 `electron/window-manager.ts`，在 `createDialogWindow` 的 `loadRenderer(win, 'dialog')` 之前加一个 blur 监听。但 blur 处理需要拿到「隐藏 dialog + 通知 character 恢复」的逻辑 —— 那逻辑在 ipc.ts 的 `dialog:close` 里。为避免循环依赖，改在 main.ts 装配时挂监听。

打开 `electron/main.ts`，在 `registerIpcHandlers(...)` 调用之后、`await session.start()` 之前加：

```ts
  // spec §3.2: clicking outside the dialog closes it. The dialog window
  // losing focus == user clicked elsewhere. Reuse the same close path as
  // Esc / character-click-toggle by faking a dialog:close.
  windows.dialog.on('blur', () => {
    if (windows && windows.dialog.isVisible()) {
      windows.dialog.hide()
      windows.character.webContents.send('character:dialog-visibility', false)
    }
  })
```

> 注意：`windows` 是 main.ts 里 `let windows: JPTWindows | null`。上面用 `windows &&` 守一下 null。把这段放在 `windows = createWindows()` 之后即可（那时 `windows` 已非 null，但 TS 仍认为可能 null，所以保留守卫）。

- [ ] **Step 3.2: tsc + 手动 smoke**

```
npx tsc --noEmit -p tsconfig.json
```

Expected: 0。手动：开对话框 → 点桌面/红方块以外任意处 → 对话框消失 + 红方块恢复走动。Esc 仍可关。再点红方块仍可重开。

> 边界：点红方块本身会先触发 dialog blur（关）再触发 character:click（又开）—— 净效果是「保持开着或快速开关」。v1 已知 character:click 是 toggle。若实测点红方块时出现闪烁，在 Step 3.1 的 blur handler 里加 50ms 去抖：用 `setTimeout` 延迟 hide，character:click 到达时清除。本 plan 默认不加，实测有问题再补（记 v1.5 follow-up）。

- [ ] **Step 3.3: Commit**

```bash
git add electron/main.ts
git commit -m "feat(dialog): clicking outside (window blur) closes the dialog"
```

---

### Task 4: 斜杠命令 /clear /copy /help（TDD 纯逻辑 + 接线）

**Files:**
- Create: `tests/slash.test.ts`
- Create: `src/dialog/slash.ts`
- Modify: `src/dialog/App.tsx`

- [ ] **Step 4.1: 写测试（先 fail）**

Create `tests/slash.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { parseSlash } from '../src/dialog/slash'

describe('parseSlash', () => {
  it('returns null for normal text', () => {
    expect(parseSlash('你好')).toBeNull()
    expect(parseSlash('  帮我查天气  ')).toBeNull()
  })
  it('parses /clear', () => {
    expect(parseSlash('/clear')).toEqual({ cmd: 'clear' })
    expect(parseSlash('  /clear  ')).toEqual({ cmd: 'clear' })
  })
  it('parses /copy', () => {
    expect(parseSlash('/copy')).toEqual({ cmd: 'copy' })
  })
  it('parses /help', () => {
    expect(parseSlash('/help')).toEqual({ cmd: 'help' })
  })
  it('unknown slash returns help', () => {
    expect(parseSlash('/wat')).toEqual({ cmd: 'help' })
  })
  it('slash with trailing text is still a command', () => {
    expect(parseSlash('/clear everything')).toEqual({ cmd: 'clear' })
  })
})
```

- [ ] **Step 4.2: Run, verify fail**

```
npm test -- slash
```

Expected: fail（模块不存在）。

- [ ] **Step 4.3: 实现 slash.ts**

Create `src/dialog/slash.ts`:

```ts
export type SlashCmd = 'clear' | 'copy' | 'help'

export interface SlashParsed {
  cmd: SlashCmd
}

const HELP_TEXT = `可用命令：
/clear — 清空当前对话（不删历史文件）
/copy — 复制最后一条 JPT 回复
/help — 显示这个帮助`

export function parseSlash(input: string): SlashParsed | null {
  const t = input.trim()
  if (!t.startsWith('/')) return null
  const word = t.slice(1).split(/\s+/)[0].toLowerCase()
  if (word === 'clear') return { cmd: 'clear' }
  if (word === 'copy') return { cmd: 'copy' }
  return { cmd: 'help' }
}

export function slashHelpText(): string {
  return HELP_TEXT
}
```

- [ ] **Step 4.4: Run, verify pass**

```
npm test -- slash
```

Expected: 6 过。

- [ ] **Step 4.5: dialog App.tsx 接斜杠命令**

打开 `src/dialog/App.tsx`。顶部 import 加：

```tsx
import { parseSlash, slashHelpText } from './slash'
```

把 `onSend` 函数替换为：

```tsx
  const onSend = () => {
    const text = input.trim()
    if (!text || busy || !ready) return
    const slash = parseSlash(text)
    if (slash) {
      setInput('')
      if (slash.cmd === 'clear') {
        setMsgs([])
        window.jpt.send('dialog:slash-clear')
      } else if (slash.cmd === 'copy') {
        const lastAssistant = [...msgs].reverse().find((m) => m.role === 'assistant') as { text: string } | undefined
        if (lastAssistant) navigator.clipboard?.writeText(lastAssistant.text).catch(() => {})
        setMsgs((p) => [...p, { role: 'assistant', text: lastAssistant ? '（已复制上一条回复）' : '（没有可复制的回复）' }])
      } else {
        setMsgs((p) => [...p, { role: 'assistant', text: slashHelpText() }])
      }
      return
    }
    setMsgs((p) => [...p, { role: 'user', text }])
    setInput('')
    setBusy(true)
    window.jpt.send('dialog:user-send', text)
  }
```

- [ ] **Step 4.6: main 侧 /clear 清 Claude 会话上下文**

`/clear` 不光清 UI，也要清 Claude 端上下文。打开 `electron/ipc.ts`，在 `dialog:user-send` handler 之后加：

```ts
  // Dialog → main: /clear — reset Claude's conversation context.
  // Cheapest correct way: terminate + restart the session (it re-loads
  // workdir/CLAUDE.md persona fresh, no history replay).
  ipcMain.on('dialog:slash-clear', async () => {
    session.terminate()
    await session.start()
  })
```

> `session.terminate()` 和 `start()` 在 `AgentSession` 接口里已有（v0 Task 9 定义）。`start()` 是 idempotent + 重置 readyFired（v1 已改）。

- [ ] **Step 4.7: tsc + 全测 + 手动**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc 0；43 测试过（37 + 6 slash）。手动：对话框输入 `/help` 见帮助；`/clear` 清空且后续提问是全新上下文；`/copy` 复制上一条。

- [ ] **Step 4.8: Commit**

```bash
git add tests/slash.test.ts src/dialog/slash.ts src/dialog/App.tsx electron/ipc.ts
git commit -m "feat(dialog): slash commands /clear /copy /help (TDD)"
```

---

### Task 5: 设置里改人格真正落地到 workdir/CLAUDE.md（修 v1 follow-up #9）

**Files:**
- Modify: `electron/agent/workdir.ts`
- Modify: `electron/ipc.ts`

v1 acceptance #9：设置窗 persona 编辑器写进 electron-store，但**没**重写 `workdir/CLAUDE.md`，所以改了不生效。

- [ ] **Step 5.1: workdir.ts 暴露 persona 写入函数**

打开 `electron/agent/workdir.ts`。在 `ensureWorkdir` 之后追加导出：

```ts
import * as os from 'node:os' // already may be unused; remove if tsc complains. (Actually not needed — delete this line if present.)

/**
 * Overwrite <workdir>/CLAUDE.md with an explicit persona doc. Called when the
 * user edits the persona in the settings window. Empty/whitespace input is
 * ignored (keeps whatever's there — never blank out the persona).
 * Returns true if written.
 */
export function writePersona(basePath: string, personaDoc: string): boolean {
  if (!personaDoc || !personaDoc.trim()) return false
  const workdir = path.join(basePath, 'workdir')
  fs.mkdirSync(workdir, { recursive: true })
  fs.writeFileSync(path.join(workdir, 'CLAUDE.md'), personaDoc, 'utf-8')
  return true
}
```

> 删掉上面那行 `import * as os` —— 它是误加的提示，`workdir.ts` 不需要 os。最终 import 区只要 `fs` + `path`。

- [ ] **Step 5.2: ensureWorkdir 启动时若 config 有 personaDoc 则用它**

`ensureWorkdir` 当前签名 `ensureWorkdir(basePath: string)`，启动时写占位。改成可选接受 personaDoc：把 `ensureWorkdir` 函数签名改为：

```ts
export function ensureWorkdir(basePath: string, personaDoc?: string): string {
  const workdir = path.join(basePath, 'workdir')
  fs.mkdirSync(workdir, { recursive: true })
  // v1.5: also scaffold the .claude/skills dir so the WeChat persona skill
  // (project-level, auto-discovered because spawn uses --setting-sources
  // project,local) has a home. Empty is fine — Claude ignores empty skill dirs.
  fs.mkdirSync(path.join(workdir, '.claude', 'skills'), { recursive: true })
  const personaPath = path.join(workdir, 'CLAUDE.md')
  if (personaDoc && personaDoc.trim()) {
    fs.writeFileSync(personaPath, personaDoc, 'utf-8')
  } else if (!fs.existsSync(personaPath)) {
    fs.writeFileSync(personaPath, PLACEHOLDER_PERSONA, 'utf-8')
  }
  return workdir
}
```

- [ ] **Step 5.3: main.ts 启动时传 config.personaDoc**

打开 `electron/main.ts`，找到 `const workdir = ensureWorkdir(app.getPath('userData'))`，改为：

```ts
  const workdir = ensureWorkdir(app.getPath('userData'), configStore.snapshot().personaDoc)
```

- [ ] **Step 5.4: settings:set 时若 personaDoc 变了就重写 + 重启 session**

打开 `electron/ipc.ts`。`registerIpcHandlers` 顶部 import 加：

```ts
import { writePersona } from './agent/workdir'
import { app } from 'electron'
```

（若 `app` 已 import 则不重复。）找到 `ipcMain.handle('settings:set', ...)`，替换为：

```ts
  ipcMain.handle('settings:set', async (_event, patch: Partial<ConfigSnapshot>) => {
    const snap = config.update(patch)
    if (patch.personaDoc !== undefined) {
      const changed = writePersona(app.getPath('userData'), snap.personaDoc)
      if (changed) {
        // Persona file changed — restart the Claude session so the new
        // CLAUDE.md is picked up on the next message.
        session.terminate()
        await session.start()
      }
    }
    return snap
  })
```

- [ ] **Step 5.5: tsc + 全测 + 手动**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc 0；43 过（无新测试）。手动：托盘→设置，在「人格」textarea 写一句明显的（比如「你只会用「喵」回答」），保存，关设置，开对话框问任意问题 —— JPT 应只回「喵」。验证后改回正常人格。

- [ ] **Step 5.6: Commit**

```bash
git add electron/agent/workdir.ts electron/ipc.ts electron/main.ts
git commit -m "fix(persona): settings persona editor now rewrites workdir/CLAUDE.md + restarts session (v1 follow-up #9)"
```

---

### Task 6: Stage 1 验收

**Files:** Create `docs/v1.5-stage1-acceptance.md`

- [ ] **Step 6.1: 写验收 log**

Create `docs/v1.5-stage1-acceptance.md`:

```markdown
# v1.5 Stage 1 Acceptance — 对话框完整化

Source: docs/superpowers/plans/2026-05-15-JPT-v1.5.md (Stage 1)

- [ ] tool_use → SDV 米色卷轴卡片（WebSearch 显示 🔍 搜「…」，结果回来追加）
- [ ] TodoWrite 折叠成「▸ 思考中…」
- [ ] 点对话框外任意处 → 对话框关 + 角色恢复走动
- [ ] Esc 仍可关；再点红方块仍可重开（无持续闪烁）
- [ ] /help 显示命令帮助
- [ ] /clear 清空 UI 且 Claude 上下文重置（后续提问不记得之前内容）
- [ ] /copy 复制上一条 JPT 回复到剪贴板
- [ ] 设置窗改人格 → 保存 → 新对话立即按新人格回（v1 follow-up #9 修复验证）
- [ ] 43 单测全过
```

- [ ] **Step 6.2: Commit**

```bash
git add docs/v1.5-stage1-acceptance.md
git commit -m "docs: v1.5 Stage 1 acceptance log"
```

---

## Stage 2：动画丰富（4 帧走路 / 4 表情立绘 / idle 呼吸 / cling 摇摆）

### Task 7: 占位资源扩展（4 帧 sheet + 3 表情立绘 + 静音音效）

**Files:**
- Modify: `scripts/gen-placeholder-art.ps1`
- Create (via script): `assets/sprites/jpt-walk.png`(384×128 4-frame), `jpt-portrait-{smile,think,confused}.png`, `assets/sounds/{complete,click,type}.mp3`

- [ ] **Step 7.1: 改占位脚本**

打开 `scripts/gen-placeholder-art.ps1`。在现有 `Save-RedSquare 96 128 ...jpt-walk.png` 行替换 + 追加：

```powershell
# v1.5: walk sprite is now a 4-frame horizontal sheet (each frame 96x128 → 384x128).
# Placeholder = 4 red squares with slightly different brightness so frame cycling is visible.
function Save-WalkSheet {
  param([string]$path)
  $bmp = New-Object System.Drawing.Bitmap 384, 128
  $g = [System.Drawing.Graphics]::FromImage($bmp)
  $shades = @(60, 90, 60, 30)
  for ($i = 0; $i -lt 4; $i++) {
    $brush = New-Object System.Drawing.SolidBrush ([System.Drawing.Color]::FromArgb(255, 240, $shades[$i], $shades[$i]))
    $g.FillRectangle($brush, ($i*96), 0, 96, 128)
    $brush.Dispose()
  }
  $bmp.Save($path, [System.Drawing.Imaging.ImageFormat]::Png)
  $g.Dispose(); $bmp.Dispose()
  Write-Host "wrote $path (384x128, 4 frames)"
}

Save-WalkSheet "$root\assets\sprites\jpt-walk.png"
Save-RedSquare 256 256 "$root\assets\sprites\jpt-portrait.png"
Save-RedSquare 256 256 "$root\assets\sprites\jpt-portrait-smile.png"
Save-RedSquare 256 256 "$root\assets\sprites\jpt-portrait-think.png"
Save-RedSquare 256 256 "$root\assets\sprites\jpt-portrait-confused.png"

# Silent placeholder mp3s (44 bytes — valid empty MPEG frame). Real CC0 SDV
# sounds replace these later (see docs note in Task 9).
New-Item -Force -ItemType Directory "$root\assets\sounds" | Out-Null
$silent = [byte[]](0xFF,0xFB,0x90,0x64) + (New-Object byte[] 40)
foreach ($n in @('complete','click','type')) {
  [System.IO.File]::WriteAllBytes("$root\assets\sounds\$n.mp3", $silent)
  Write-Host "wrote $root\assets\sounds\$n.mp3 (silent placeholder)"
}
```

> 注意：原脚本里那一行 `Save-RedSquare 96 128 "$root\assets\sprites\jpt-walk.png"` 删掉（被 `Save-WalkSheet` 取代）；原 `Save-RedSquare 256 256 ...jpt-portrait.png` 保留（上面重复写了也无妨，保留一处即可）。

- [ ] **Step 7.2: 跑脚本**

```powershell
powershell -ExecutionPolicy Bypass -File scripts\gen-placeholder-art.ps1
```

Expected: 输出 `jpt-walk.png (384x128, 4 frames)` + 4 张 portrait + 3 个 silent mp3。

- [ ] **Step 7.3: Commit**

```bash
git add scripts/gen-placeholder-art.ps1 assets/sprites/ assets/sounds/
git commit -m "chore(assets): 4-frame walk sheet + 3 expression portraits + silent sound placeholders"
```

---

### Task 8: 4 帧走路帧选择（TDD 纯逻辑）+ 接到 character

**Files:**
- Create: `tests/sprite-sheet.test.ts`
- Create: `src/character/sprite-sheet.ts`
- Modify: `src/character/App.tsx`

- [ ] **Step 8.1: 写测试**

Create `tests/sprite-sheet.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { walkFrame } from '../src/character/sprite-sheet'

describe('walkFrame', () => {
  it('idle always shows frame 0', () => {
    expect(walkFrame('idle', 0)).toBe(0)
    expect(walkFrame('idle', 999999)).toBe(0)
  })
  it('held / cling / fall show frame 0 (no walk cycle)', () => {
    expect(walkFrame('held', 12345)).toBe(0)
    expect(walkFrame('cling', 12345)).toBe(0)
    expect(walkFrame('fall', 12345)).toBe(0)
  })
  it('walk cycles 0..3 every 120ms each (480ms full cycle)', () => {
    expect(walkFrame('walk', 0)).toBe(0)
    expect(walkFrame('walk', 119)).toBe(0)
    expect(walkFrame('walk', 120)).toBe(1)
    expect(walkFrame('walk', 240)).toBe(2)
    expect(walkFrame('walk', 360)).toBe(3)
    expect(walkFrame('walk', 480)).toBe(0)
    expect(walkFrame('walk', 600)).toBe(1)
  })
})
```

- [ ] **Step 8.2: Run, verify fail**

```
npm test -- sprite-sheet
```

Expected: fail.

- [ ] **Step 8.3: 实现**

Create `src/character/sprite-sheet.ts`:

```ts
import type { CharMode } from './state-machine'

export const FRAME_COUNT = 4
export const FRAME_MS = 120

/**
 * Which sprite-sheet frame (0..3) to show. Only `walk` cycles; every other
 * mode is the static frame 0. `nowMs` is any monotonic clock (performance.now).
 */
export function walkFrame(mode: CharMode, nowMs: number): number {
  if (mode !== 'walk') return 0
  return Math.floor(nowMs / FRAME_MS) % FRAME_COUNT
}
```

- [ ] **Step 8.4: Run, verify pass**

```
npm test -- sprite-sheet
```

Expected: 3 过。

- [ ] **Step 8.5: character App.tsx 用 sheet 渲染**

打开 `src/character/App.tsx`。顶部 import 加：

```tsx
import { walkFrame, FRAME_COUNT } from './sprite-sheet'
```

把渲染 return 的 `<div bg:red><img .../></div>` 替换为（sprite sheet 是横向 4 帧，用 `object-fit` + 负 `margin` 平移取帧；外层 div 裁剪）：

```tsx
  const frame = walkFrame(state.mode, performance.now())
  return (
    <div
      style={{
        width: 96,
        height: 128,
        background: 'red', // visible fallback if PNG fails
        transform,
        transformOrigin: 'center',
        userSelect: 'none',
        cursor: 'pointer',
        overflow: 'hidden',
        position: 'relative',
      }}
    >
      <img
        src={spriteUrl}
        alt="JPT"
        draggable={false}
        style={{
          position: 'absolute',
          left: `${-frame * 96}px`,
          top: 0,
          width: `${FRAME_COUNT * 96}px`,
          height: 128,
          display: 'block',
          imageRendering: 'pixelated',
        }}
      />
    </div>
  )
```

> 帧切换靠 rAF 已有的每帧重渲染驱动（`state` 每 tick 变 → 重渲染 → `performance.now()` 重新算 `frame`）。idle 时 state 不变不重渲染 —— 但 idle 帧恒为 0，无需动，正确。

- [ ] **Step 8.6: tsc + 全测 + 手动**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc 0；46 过（43 + 3）。手动：红方块走动时 4 个红块亮度循环（占位可见帧切换）；停下时恒为第 1 帧。真 4 帧 sprite sheet 换上后即为走路动画。

- [ ] **Step 8.7: Commit**

```bash
git add tests/sprite-sheet.test.ts src/character/sprite-sheet.ts src/character/App.tsx
git commit -m "feat(character): 4-frame walk sprite sheet cycling (TDD)"
```

---

### Task 9: idle 呼吸 + cling 摇摆（CSS 动画）

**Files:**
- Modify: `src/character/App.tsx`

spec §3.1：idle「轻微垂直浮动（呼吸）」；cling「轻微摇摆」。纯 CSS keyframe，不进状态机。

- [ ] **Step 9.1: 注入 keyframes + 按 mode 加 animation**

打开 `src/character/App.tsx`。在组件 return 之前，把现有 `transform` 计算保留，新增一个 `animation` 字符串：

```tsx
  const animation =
    state.mode === 'idle' ? 'jpt-breathe 2.6s ease-in-out infinite'
    : state.mode === 'cling' ? 'jpt-sway 1.8s ease-in-out infinite'
    : 'none'
```

在外层 `<div>` 的 style 里加 `animation`（和 transform 并存：transform 控翻转/旋转/squash，animation 控呼吸/摇摆的细微位移，二者叠加 OK 因为 animation 用 translate，transform 用 scale/rotate —— 但 CSS 同一元素 transform 和 animation 的 transform 会冲突。解决：把 sprite `<img>` 包一层，外层 div 用 transform，内层 wrapper 用 animation）。

具体：把 Task 8 的结构再包一层。最终 return：

```tsx
  return (
    <div
      style={{
        width: 96,
        height: 128,
        transform,            // facing flip / cling rotate / squash
        transformOrigin: 'center',
        userSelect: 'none',
        cursor: 'pointer',
      }}
    >
      <div
        style={{
          width: '100%',
          height: '100%',
          background: 'red',
          overflow: 'hidden',
          position: 'relative',
          animation,          // breathe / sway — translate only, no transform clash
        }}
      >
        <img
          src={spriteUrl}
          alt="JPT"
          draggable={false}
          style={{
            position: 'absolute',
            left: `${-frame * 96}px`,
            top: 0,
            width: `${FRAME_COUNT * 96}px`,
            height: 128,
            display: 'block',
            imageRendering: 'pixelated',
          }}
        />
      </div>
      <style>{`
        @keyframes jpt-breathe {
          0%,100% { transform: translateY(0); }
          50%     { transform: translateY(-2px); }
        }
        @keyframes jpt-sway {
          0%,100% { transform: translateX(0) rotate(0deg); }
          50%     { transform: translateX(1px) rotate(2deg); }
        }
      `}</style>
    </div>
  )
```

- [ ] **Step 9.2: tsc + 手动**

```
npx tsc --noEmit -p tsconfig.json
```

Expected: 0。手动：idle 时红块轻微上下浮动（2px，2.6s 周期）；拖到右墙挂壁时轻微摇摆。走动 / held / fall 时无 animation。

- [ ] **Step 9.3: Commit**

```bash
git add src/character/App.tsx
git commit -m "feat(character): idle breathing + cling sway (CSS keyframes)"
```

---

### Task 10: 4 表情立绘 + 事件驱动切换

**Files:**
- Modify: `electron/ipc.ts`
- Modify: `src/dialog/PortraitPanel.tsx`
- Modify: `src/dialog/App.tsx`

spec §2.2：「多张立绘（默认/微笑/思考/困惑），按 tool_use / 完成 / 错误等事件切换」。

- [ ] **Step 10.1: PortraitPanel 按 expression prop 切图**

打开 `src/dialog/PortraitPanel.tsx`。顶部 import 4 张：

```tsx
import portraitDefault from '../../assets/sprites/jpt-portrait.png'
import portraitSmile from '../../assets/sprites/jpt-portrait-smile.png'
import portraitThink from '../../assets/sprites/jpt-portrait-think.png'
import portraitConfused from '../../assets/sprites/jpt-portrait-confused.png'

export type Expression = 'default' | 'smile' | 'think' | 'confused'

const EXPR_SRC: Record<Expression, string> = {
  default: portraitDefault,
  smile: portraitSmile,
  think: portraitThink,
  confused: portraitConfused,
}
```

`PortraitPanel` 的 props 加 `expression: Expression`，渲染的 `<img src=...>` 改为 `src={EXPR_SRC[expression]}`。`PortraitPanelProps` 接口加 `expression: Expression`。其余样式不动。

- [ ] **Step 10.2: dialog App.tsx 维护 expression state**

打开 `src/dialog/App.tsx`。加 state + import：

```tsx
import type { Expression } from './PortraitPanel'
```

组件内加：

```tsx
  const [expression, setExpression] = useState<Expression>('default')
```

在事件 useEffect 里联动：
- 收到 `dialog:tool-use` → `setExpression('think')`
- 收到 `dialog:turn-complete` → `setExpression('smile')`
- 收到 `dialog:error` → `setExpression('confused')`
- 用户发消息（`onSend` 里 `window.jpt.send('dialog:user-send', text)` 之后）→ `setExpression('think')`

具体：在 `offToolUse` 的 setter 里追加 `setExpression('think')`；`offComplete` 里追加 `setExpression('smile')`；`offError` 里追加 `setExpression('confused')`；`onSend` 的非斜杠分支 `setBusy(true)` 之后加 `setExpression('think')`。

`<PortraitPanel name="JPT" />` 改为 `<PortraitPanel name="JPT" expression={expression} />`。

- [ ] **Step 10.3: tsc + 手动**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc 0；46 过。手动（占位 4 张都是红块，先验证不报错 + 切换逻辑触发；真图换上后能看出表情）：发消息→思考脸，回复完→微笑脸，触发错误→困惑脸。

- [ ] **Step 10.4: Commit**

```bash
git add src/dialog/PortraitPanel.tsx src/dialog/App.tsx
git commit -m "feat(dialog): 4 expression portraits switched by tool_use/complete/error events"
```

---

### Task 11: Stage 2 验收

**Files:** Create `docs/v1.5-stage2-acceptance.md`

- [ ] **Step 11.1: 写验收 log**

Create `docs/v1.5-stage2-acceptance.md`:

```markdown
# v1.5 Stage 2 Acceptance — 动画丰富

Source: docs/superpowers/plans/2026-05-15-JPT-v1.5.md (Stage 2)

- [ ] 走路时 4 帧循环（占位：4 块亮度轮播；真图：走路动画）
- [ ] idle 时呼吸（2px 垂直浮动，~2.6s）
- [ ] cling 挂壁时摇摆
- [ ] held / fall 无多余动画
- [ ] 立绘随事件切换：发消息/工具→思考，回复完→微笑，错误→困惑
- [ ] 占位 4 张表情都是红块也不报错；真图换上后表情可辨
- [ ] 46 单测全过
```

- [ ] **Step 11.2: Commit**

```bash
git add docs/v1.5-stage2-acceptance.md
git commit -m "docs: v1.5 Stage 2 acceptance log"
```

---

## Stage 3：生命感（音效 + 主动陪伴）

### Task 12: 音效（complete / click / type）+ soundsEnabled 接线

**Files:**
- Create: `src/dialog/sounds.ts`
- Modify: `src/dialog/App.tsx`
- Modify: `electron/ipc.ts`
- Modify: `src/character/App.tsx`

- [ ] **Step 12.1: sounds.ts —— 一个极简播放器**

Create `src/dialog/sounds.ts`:

```ts
import completeUrl from '../../assets/sounds/complete.mp3?url'
import clickUrl from '../../assets/sounds/click.mp3?url'
import typeUrl from '../../assets/sounds/type.mp3?url'

const URLS = { complete: completeUrl, click: clickUrl, type: typeUrl } as const
export type SoundName = keyof typeof URLS

let enabled = true
export function setSoundsEnabled(on: boolean) { enabled = on }

const cache: Partial<Record<SoundName, HTMLAudioElement>> = {}

export function playSound(name: SoundName) {
  if (!enabled) return
  let a = cache[name]
  if (!a) {
    a = new Audio(URLS[name])
    a.volume = 0.4
    cache[name] = a
  }
  // Restart even if mid-play (rapid type sounds).
  a.currentTime = 0
  a.play().catch(() => { /* autoplay/format errors are non-fatal */ })
}
```

- [ ] **Step 12.2: dialog App.tsx 播音 + 读 soundsEnabled**

打开 `src/dialog/App.tsx`。import：

```tsx
import { playSound, setSoundsEnabled } from './sounds'
```

挂载时读配置：在已有的 `agent:is-ready` query useEffect 里追加（或新开一个 useEffect）：

```tsx
  useEffect(() => {
    window.jpt.invoke<{ soundsEnabled: boolean }>('settings:get').then((c) => setSoundsEnabled(c.soundsEnabled))
    const off = window.jpt.on('settings:sounds-changed', (...a: unknown[]) => setSoundsEnabled(Boolean(a[0])))
    return () => { off() }
  }, [])
```

播放点：
- `offComplete`（turn-complete）里加 `playSound('complete')`
- `onSend` 非斜杠分支发送后加 `playSound('click')`
- 流式 token 到达（`offToken` setter 里）加 `playSound('type')` —— 但每 token 一响太吵，加节流：模块级 `let lastType = 0; if (Date.now()-lastType>140){ lastType=Date.now(); playSound('type') }`

- [ ] **Step 12.3: settings:set 改 soundsEnabled 时广播**

打开 `electron/ipc.ts`。在 `settings:set` handler 里（Task 5 改过的那个），`const snap = config.update(patch)` 之后加：

```ts
    if (patch.soundsEnabled !== undefined) {
      windows.dialog.webContents.send('settings:sounds-changed', snap.soundsEnabled)
    }
```

- [ ] **Step 12.4: tsc + 手动**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc 0；46 过。手动（占位是静音 mp3，验证不报错 + 不卡）：发消息无报错；托盘关声音后再发消息无声（占位本来就无声，看 console 无 Audio 报错即可）。真音效换上后听感对。

- [ ] **Step 12.5: Commit**

```bash
git add src/dialog/sounds.ts src/dialog/App.tsx electron/ipc.ts
git commit -m "feat(sounds): complete/click/type SFX with soundsEnabled wiring"
```

> **真音效来源（开发者后补，换文件即生效）：** itch.io 搜 "CC0 UI sound" / "8-bit blip"，或 Kenney.nl 的 "Interface Sounds"（CC0）。挑 3 个短音（完成音 ~400ms、点击 ~80ms、打字 ~40ms），转 mp3 覆盖 `assets/sounds/{complete,click,type}.mp3`。SDV 本体音效有版权，**不要**直接扒。

---

### Task 13: 主动陪伴定时器 + 文案表（TDD 纯逻辑 + 接线）

**Files:**
- Create: `tests/proactive.test.ts`
- Create: `electron/proactive.ts`
- Create: `src/shared/proactive-messages.ts`
- Modify: `electron/main.ts`
- Modify: `electron/ipc.ts`
- Modify: `src/dialog/App.tsx`

spec §2.2：「主动陪伴：定时器 + 文案表（早安、喝水提醒、节日彩蛋）」。受 `config.proactiveMessages` 开关控制（v0 spec §8.1 预留了这个键，但 v1 ConfigSnapshot 没加 —— 本 task 补上）。

- [ ] **Step 13.1: 文案表**

Create `src/shared/proactive-messages.ts`:

```ts
export interface ProactiveMsg {
  /** local hour (0-23) lower bound, inclusive */
  fromHour: number
  /** local hour upper bound, exclusive */
  toHour: number
  text: string
}

/** First match wins. Picked by current local hour. */
export const PROACTIVE_MESSAGES: ProactiveMsg[] = [
  { fromHour: 6, toHour: 10, text: '小屿早上好呀，今天也要好好的 🌿' },
  { fromHour: 10, toHour: 12, text: '上午过半啦，记得喝口水 ☕' },
  { fromHour: 12, toHour: 14, text: '该吃午饭咯，别饿着自己。' },
  { fromHour: 14, toHour: 18, text: '下午容易困，要不要起来走两步？' },
  { fromHour: 18, toHour: 20, text: '忙了一天，晚饭吃了吗？' },
  { fromHour: 20, toHour: 23, text: '晚上啦，别太晚睡，眼睛也要休息 🌙' },
  { fromHour: 23, toHour: 24, text: '这么晚还没睡？我陪着你，但你也早点休息。' },
  { fromHour: 0, toHour: 6, text: '凌晨了，别熬了，明天还有明天的事。' },
]
```

- [ ] **Step 13.2: 写测试**

Create `tests/proactive.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { pickProactive, shouldFire } from '../electron/proactive'

describe('pickProactive', () => {
  it('picks the message whose hour range contains the given hour', () => {
    expect(pickProactive(7)?.text).toContain('早上好')
    expect(pickProactive(13)?.text).toContain('午饭')
    expect(pickProactive(21)?.text).toContain('晚上')
    expect(pickProactive(2)?.text).toContain('凌晨')
  })
  it('every hour 0..23 maps to exactly one message', () => {
    for (let h = 0; h < 24; h++) expect(pickProactive(h)).not.toBeNull()
  })
})

describe('shouldFire', () => {
  it('false if disabled', () => {
    expect(shouldFire(false, 0, 999999999)).toBe(false)
  })
  it('false before the interval elapses', () => {
    expect(shouldFire(true, 1000, 1000 + 60_000)).toBe(false)
  })
  it('true once at least 90 min since last fire', () => {
    expect(shouldFire(true, 0, 90 * 60_000)).toBe(true)
    expect(shouldFire(true, 0, 90 * 60_000 + 1)).toBe(true)
  })
})
```

- [ ] **Step 13.3: Run, verify fail**

```
npm test -- proactive
```

Expected: fail.

- [ ] **Step 13.4: 实现 proactive.ts**

Create `electron/proactive.ts`:

```ts
import { PROACTIVE_MESSAGES, ProactiveMsg } from '../src/shared/proactive-messages'

const MIN_INTERVAL_MS = 90 * 60_000 // 90 minutes between proactive nudges

/** Message for a given local hour (0-23). First range match wins. */
export function pickProactive(hour: number): ProactiveMsg | null {
  for (const m of PROACTIVE_MESSAGES) {
    if (hour >= m.fromHour && hour < m.toHour) return m
  }
  return null
}

/** Whether a proactive message is due. `nowMs` / `lastFireMs` are epoch ms. */
export function shouldFire(enabled: boolean, lastFireMs: number, nowMs: number): boolean {
  if (!enabled) return false
  return nowMs - lastFireMs >= MIN_INTERVAL_MS
}
```

- [ ] **Step 13.5: Run, verify pass**

```
npm test -- proactive
```

Expected: 5 过。

- [ ] **Step 13.6: ConfigSnapshot 加 proactiveMessages 键**

打开 `src/shared/config.ts`。`ConfigSnapshot` 接口加 `proactiveMessages: boolean`；`DEFAULT_CONFIG` 加 `proactiveMessages: false`（默认关 —— spec §8.1 默认 false）。

`electron/config-store.ts` 的 `ConfigStore.snapshot()` 读取列表里加一行 `proactiveMessages: this.store.get('proactiveMessages', DEFAULT_CONFIG.proactiveMessages)!`（照现有字段格式补）。`tests/config-store.test.ts` 的 `FakeStore` 无需改（泛型驱动）；但「returns defaults」测试可加一行断言 `expect(snap.proactiveMessages).toBe(false)` —— 加上保持覆盖。

- [ ] **Step 13.7: main.ts 装主动陪伴定时器**

打开 `electron/main.ts`。import：

```ts
import { pickProactive, shouldFire } from './proactive'
```

在 `whenReady` 内、`await session.start()` 之后加：

```ts
  // Proactive companionship: every 10 min check whether a nudge is due.
  let lastProactive = Date.now()
  setInterval(() => {
    const cfg = configStore.snapshot()
    if (!shouldFire(cfg.proactiveMessages, lastProactive, Date.now())) return
    if (!windows) return
    const msg = pickProactive(new Date().getHours())
    if (!msg) return
    lastProactive = Date.now()
    // Show the dialog (if not already) and push the proactive line as if JPT spoke.
    if (!windows.dialog.isVisible()) {
      // reuse toggleDialog's open path
      // (import toggleDialog already present for tray)
    }
    windows.dialog.webContents.send('dialog:proactive', msg.text)
  }, 10 * 60_000)
```

> 为了不打架，主动消息**只在对话框已经开着时**推（避免半夜弹窗惊到人）。删掉上面 `if (!windows.dialog.isVisible())` 那个空 if 块，改为：若对话框没开，跳过本次（`if (!windows.dialog.isVisible()) return` 放在 send 之前）。最终那段是：

```ts
  let lastProactive = Date.now()
  setInterval(() => {
    const cfg = configStore.snapshot()
    if (!shouldFire(cfg.proactiveMessages, lastProactive, Date.now())) return
    if (!windows || !windows.dialog.isVisible()) return
    const msg = pickProactive(new Date().getHours())
    if (!msg) return
    lastProactive = Date.now()
    windows.dialog.webContents.send('dialog:proactive', msg.text)
  }, 10 * 60_000)
```

- [ ] **Step 13.8: dialog App.tsx 渲染主动消息**

打开 `src/dialog/App.tsx`。在事件 useEffect 里加监听：

```tsx
    const offProactive = window.jpt.on('dialog:proactive', (...a: unknown[]) => {
      setMsgs((p) => [...p, { role: 'assistant', text: String(a[0]) }])
      setExpression('smile')
    })
```

cleanup 数组加 `offProactive()`。

- [ ] **Step 13.9: tsc + 全测 + 手动**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc 0；51 过（46 + 5 proactive）。手动验证麻烦（要等 90 min）—— 临时把 `MIN_INTERVAL_MS` 改 `30_000`、`setInterval` 改 `5_000` 跑一次看对话框开着时会冒主动消息，验证后改回。设置里 `proactiveMessages` 默认 false，需要在 settings 窗或直接改 config.json 打开。

- [ ] **Step 13.10: Commit**

```bash
git add tests/proactive.test.ts electron/proactive.ts src/shared/proactive-messages.ts src/shared/config.ts electron/config-store.ts tests/config-store.test.ts electron/main.ts src/dialog/App.tsx
git commit -m "feat(proactive): time-based companionship nudges (TDD, opt-in)"
```

---

### Task 14: Stage 3 验收

**Files:** Create `docs/v1.5-stage3-acceptance.md`

- [ ] **Step 14.1: 写验收 log**

Create `docs/v1.5-stage3-acceptance.md`:

```markdown
# v1.5 Stage 3 Acceptance — 生命感

Source: docs/superpowers/plans/2026-05-15-JPT-v1.5.md (Stage 3)

- [ ] 占位静音 mp3 不报错、不卡 UI
- [ ] 真音效换上：发送 click、回复完 complete、流式打字音（节流不刺耳）
- [ ] 托盘关声音后无声；开声音后恢复
- [ ] proactiveMessages 默认关；开后、对话框开着时按时段冒主动消息（早安/喝水/夜深）
- [ ] 主动消息不会在对话框关着时弹（不惊扰）
- [ ] 51 单测全过
```

- [ ] **Step 14.2: Commit**

```bash
git add docs/v1.5-stage3-acceptance.md
git commit -m "docs: v1.5 Stage 3 acceptance log"
```

---

## Stage 4：微信人格 skill 脚手架

spec §2.2 + CLAUDE.md §3：微信聊天记录 → `workdir/.claude/skills/JPT-wechat/SKILL.md`，**按需触发**，作为基础人格（CLAUDE.md）的「风味增强」，不替代。开发者**手工**做抽取，本 stage 只搭脚手架 + 写指南。Task 5 已让 `ensureWorkdir` 建 `workdir/.claude/skills/` 目录。v1 spawn 用 `--setting-sources project,local` —— project 级覆盖 workdir/.claude/，所以这里放的 skill 会被自动发现。

### Task 15: SKILL.md 模板 + 抽取指南

**Files:**
- Modify: `electron/agent/workdir.ts`
- Create: `docs/wechat-persona-guide.md`

- [ ] **Step 15.1: ensureWorkdir 落 SKILL.md 模板（不覆盖已存在）**

打开 `electron/agent/workdir.ts`。在文件顶部 `PLACEHOLDER_PERSONA` 常量之后加：

```ts
const WECHAT_SKILL_TEMPLATE = `---
name: JPT-wechat
description: Use when replying to 小屿 to flavor tone with real chat-history voice — never replaces the base persona in CLAUDE.md, only adds slang, inside jokes, and speech rhythm extracted from past WeChat conversations.
---

# JPT 微信语气增强（占位 — 开发者送出前替换）

下面是从微信聊天记录抽出的「我和小屿之间的真实语气样本」。
基础人格在 workdir/CLAUDE.md，这里只做风味增强：怎么开玩笑、口头禅、共同回忆梗。

## 口头禅 / 高频说法
- （占位：替换成真实高频短语，例如「嗯呐」「先这样」「我看看哈」）

## 内部梗 / 共同回忆
- （占位：替换成只有你俩懂的梗，每条一句话上下文）

## 语气样本（原话片段，去隐私）
- （占位：贴 5-10 条真实对话片段，体现节奏和温度）
`
```

在 `ensureWorkdir` 函数里，建完 `.claude/skills` 目录之后、`return workdir` 之前加：

```ts
  const skillDir = path.join(workdir, '.claude', 'skills', 'JPT-wechat')
  fs.mkdirSync(path.join(skillDir, 'references'), { recursive: true })
  const skillPath = path.join(skillDir, 'SKILL.md')
  if (!fs.existsSync(skillPath)) {
    fs.writeFileSync(skillPath, WECHAT_SKILL_TEMPLATE, 'utf-8')
  }
```

- [ ] **Step 15.2: 抽取指南文档**

Create `docs/wechat-persona-guide.md`:

```markdown
# 微信聊天记录 → JPT 人格抽取指南（开发者手工，送出前做）

> 配套：spec §2.2 / CLAUDE.md §3。零代码改动 —— 只编辑
> %APPDATA%\jpt\workdir\.claude\skills\JPT-wechat\SKILL.md（首启已生成模板）。
> 基础人格在 workdir\CLAUDE.md（settings 窗可编辑）；这里只做「风味增强」。

## 为什么分两层
- CLAUDE.md = always-on 基础人格（关系、风格、擅长、边界）—— 每轮都注入
- SKILL.md = 按需触发的语气增强 —— Claude 检测到「在跟小屿聊天」相关上下文才激活
  好处：基础人格稳定，语气样本不污染每一轮 token，且可单独迭代

## 步骤

1. **导出微信记录**：微信 PC 版 → 备份聊天记录，或用开源工具
   (WeChatMsg / PyWxDump 等) 导出和小屿的对话为文本。**只导你俩的**。

2. **脱敏**：删手机号 / 地址 / 第三方隐私 / 真名（如果不想出现）。
   保留语气、梗、节奏。

3. **三类提炼**，填进 SKILL.md 对应小节：
   - **口头禅 / 高频说法**：你回她时反复出现的短语、语气词、句尾习惯
   - **内部梗 / 共同回忆**：只有你俩懂的梗，每条配一句话上下文（让 Claude 用得对）
   - **语气样本**：5-10 条真实片段（去隐私），体现温度和节奏，不要太长

4. **写法要点**：
   - 不要写成「JPT 应该温柔」这种抽象指令（那是 CLAUDE.md 的事）
   - 要写成「具体怎么说」：原话、口癖、什么场合说什么
   - description frontmatter 要准确（决定 Claude 何时激活这个 skill）

5. **测试**：跑 dev / 装好的 app，跟 JPT 聊几句，看语气是否对味。
   不对就回来调 SKILL.md（改完重开对话框或 /clear 让会话刷新）。

## 注意
- SKILL.md 文件本身**不要进 git 仓库**（含私人语气样本）。它住在
  %APPDATA%\jpt\workdir\.claude\skills\JPT-wechat\，不在仓库内，天然隔离。
- references/ 子目录可放更长的历史话题摘录，SKILL.md 里引用文件名即可。
- 送出的安装包**不含**真实 SKILL.md（首启只生成占位模板）；真实内容你在
  小屿电脑上 / 送出前手工填，或做成「预填好 workdir」的方式随包带（看你怎么交付）。
```

- [ ] **Step 15.3: tsc + 手动**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc 0；51 过。手动：删 `%APPDATA%\jpt\workdir`，`npm run dev`，确认重新生成 `workdir\.claude\skills\JPT-wechat\SKILL.md` + `references\` + `CLAUDE.md`。跟 JPT 说「用你 skill 里的口头禅跟我打招呼」看是否激活（占位模板内容空泛，能激活即算通）。

- [ ] **Step 15.4: Commit**

```bash
git add electron/agent/workdir.ts docs/wechat-persona-guide.md
git commit -m "feat(persona): scaffold workdir WeChat skill (SKILL.md template + extraction guide)"
```

---

## Stage 5：多屏 / 任务栏 autohide 收尾（v1 follow-up #6 #7）

### Task 16: 活跃屏检测 + 任务栏 autohide 几何

**Files:**
- Modify: `electron/window-manager.ts`
- Modify: `electron/ipc.ts`
- Modify: `electron/main.ts`

spec §3.3：活跃屏 = 含任务栏的屏优先，其次主屏；任务栏 autohide 时 workArea==bounds，角色仍贴底。

- [ ] **Step 16.1: window-manager 加活跃屏 helper**

打开 `electron/window-manager.ts`。在文件顶部 `screen` 已 import。在 `createWindows` 之前加：

```ts
/**
 * The "active" display for the character: the one whose workArea differs from
 * bounds (i.e. the taskbar lives on it). Falls back to primary. spec §3.3.
 */
export function activeDisplay() {
  const all = screen.getAllDisplays()
  const withTaskbar = all.find(
    (d) => d.bounds.height - d.workArea.height > 0 || d.bounds.width - d.workArea.width > 0,
  )
  return withTaskbar ?? screen.getPrimaryDisplay()
}

/**
 * Floor geometry for a display, accounting for taskbar autohide. When autohide
 * is on, workArea == bounds, so we reserve a small safety margin so the
 * character doesn't sit under the (popping-up) taskbar. spec §3.3.
 */
export function floorGeometry() {
  const d = activeDisplay()
  const autohide =
    d.bounds.height - d.workArea.height < 4 && d.bounds.width - d.workArea.width < 4
  const safety = autohide ? 48 : 0
  return {
    leftBound: d.workArea.x,
    rightBound: d.workArea.x + d.workArea.width - CHARACTER_W,
    floorY: d.workArea.y + d.workArea.height - CHARACTER_H - safety,
  }
}
```

- [ ] **Step 16.2: createCharacterWindow 用 activeDisplay**

`createCharacterWindow` 里当前 `const display = screen.getPrimaryDisplay()`。改为 `const display = activeDisplay()`。其余不动（x/y 计算继续用 `display.workArea`）。

- [ ] **Step 16.3: get-walk-bounds 用 floorGeometry**

打开 `electron/ipc.ts`。找到 `ipcMain.handle('character:get-walk-bounds', ...)`，整个替换为：

```ts
  ipcMain.handle('character:get-walk-bounds', () => floorGeometry())
```

顶部 import 加 `import { floorGeometry } from './window-manager'`（若已 import 其它 window-manager 导出，合并到同一行）。

- [ ] **Step 16.4: 多屏变化时让 character 重新取 bounds**

打开 `electron/main.ts`。import `screen`：`import { app, Tray, BrowserWindow, Menu, screen } from 'electron'`（合并到现有 electron import）。在 `whenReady` 内、定时器之后加：

```ts
  const refreshBounds = () => windows?.character.webContents.send('character:bounds-changed')
  screen.on('display-added', refreshBounds)
  screen.on('display-removed', refreshBounds)
  screen.on('display-metrics-changed', refreshBounds)
```

- [ ] **Step 16.5: character App.tsx 响应 bounds-changed**

打开 `src/character/App.tsx`。在 bounds-fetch useEffect 里，把 invoke 抽成可复用函数并监听 `character:bounds-changed`：

```tsx
  useEffect(() => {
    let mounted = true
    const fetchBounds = () => {
      window.jpt.invoke<WalkBounds>('character:get-walk-bounds').then((b) => {
        if (!mounted) return
        boundsRef.current = b
        // keep x in range, snap y to new floor
        setState((s) => ({ ...s, x: Math.min(Math.max(s.x, b.leftBound), b.rightBound), y: b.floorY }))
      })
    }
    fetchBounds()
    const off = window.jpt.on('character:bounds-changed', fetchBounds)
    return () => { mounted = false; off() }
  }, [])
```

- [ ] **Step 16.6: tsc + 全测 + 手动**

```
npx tsc --noEmit -p tsconfig.json
npm test
```

Expected: tsc 0；51 过。手动（有外屏才能完整测）：单屏下行为不变；接外屏 / 拔外屏角色回到含任务栏的屏底；任务栏设自动隐藏后角色不被弹出的任务栏盖住（贴底略上移 48px）。

- [ ] **Step 16.7: Commit**

```bash
git add electron/window-manager.ts electron/ipc.ts electron/main.ts src/character/App.tsx
git commit -m "feat(geometry): active-screen detection + taskbar-autohide-aware floor (v1 follow-ups #6 #7)"
```

---

### Task 17: v1.5 验收 + 版本号

**Files:**
- Create: `docs/v1.5-acceptance.md`
- Modify: `package.json`

- [ ] **Step 17.1: package.json 版本**

打开 `package.json`，`"version"` 改 `"1.5.0"`。

- [ ] **Step 17.2: 全测 + 打包验证**

```
npm test
npm run build:installer
```

Expected: 51 测试过；`release/JPT-Setup-1.5.0.exe` 产出（electron-builder 缓存 v1 已填，直接成）。

- [ ] **Step 17.3: 写 v1.5 acceptance log**

Create `docs/v1.5-acceptance.md`:

```markdown
# v1.5 Acceptance Log — 灵魂注入

Source plan: docs/superpowers/plans/2026-05-15-JPT-v1.5.md
Stage logs: docs/v1.5-stage{1,2,3}-acceptance.md

## Acceptance
- [ ] Stage 1: tool_use 卡片 / 点外关 / 斜杠命令 / 设置改人格落地
- [ ] Stage 2: 4 帧走路 / idle 呼吸 / cling 摇摆 / 4 表情立绘事件切换
- [ ] Stage 3: 音效（占位静音不报错，真音效后补）/ 主动陪伴 opt-in
- [ ] Stage 4: 微信 skill 脚手架 + 抽取指南
- [ ] Stage 5: 多屏活跃屏 / 任务栏 autohide
- [ ] 51 单测全过
- [ ] npm run build:installer 产出 JPT-Setup-1.5.0.exe

## Gift-ship checklist 增量（v1 的基础上额外做）
- [ ] 真 4 帧走路 sprite sheet 替换 jpt-walk.png（见 prompts.md「v1.5 才做的」）
- [ ] 真 4 张表情立绘替换 jpt-portrait{,-smile,-think,-confused}.png
- [ ] 真 CC0 音效替换 assets/sounds/{complete,click,type}.mp3（Task 12 注释里的来源）
- [ ] 按 docs/wechat-persona-guide.md 填真实 SKILL.md（在小屿电脑 / 送出前手工）
- [ ] 重新 npm run build:installer + 干净机器 smoke test

## 已知 v1.5 之后的 follow-up
- 点红方块时 dialog blur(关) 与 character:click(开) 竞争，若实测闪烁需加去抖（Task 3 Step 3.2 备注）
- 截屏感知 / VLM 记忆（v2，spec §2.3）
- 多 provider / 多角色 / macOS（v2）
```

- [ ] **Step 17.4: Commit + push**

```bash
git add docs/v1.5-acceptance.md package.json
git commit -m "docs: v1.5 acceptance log + bump version to 1.5.0"
git push origin main
```

---

## v1.5 完成。下一步

- 送出前最后一公里：本 plan Stage 1-5 跑完 + Gift-ship checklist 增量项（真 sprite sheet / 表情 / 音效 / 微信 SKILL.md）
- v2 推广（spec §2.3）：通用化首启配置、多 provider（OpenAI 兼容 / DeepSeek / ollama）、多角色、macOS、截屏感知 / VLM 记忆

---

## Self-review notes（写完自查，已内联修正）

- **Spec §2.2 覆盖**：4 帧 sprite(T8) / 多表情立绘(T10) / 主动陪伴(T13) / 音效(T12) / 斜杠命令(T4) / 微信 SKILL.md(T15) —— 全覆盖。微信抽取本体是开发者手工（spec 明说「开发者自行实现；零代码改动」），本 plan 给脚手架 + 指南，符合 spec 意图。
- **v1 follow-up 9 条**：#1 tool_use(T2) #3 点外关(T3) #4 idle 呼吸(T9) #5 cling 摇摆(T9) #6 多屏(T16) #7 任务栏(T16) #8 音效(T12) #9 persona 落地(T5)；#2 角色冻结 v1 已做，本 plan 不含。全覆盖。
- **类型一致性**：`AgentSessionCallbacks` 加的 `onToolUse(tool,summary)` / `onToolResult(summary,isError)` 在 T1 定义、T2 ipc.ts 使用，签名一致。`Expression` 在 T10 PortraitPanel 定义并被 dialog App import，名字一致。`walkFrame(mode,nowMs)` T8 定义即用。`writePersona(basePath,personaDoc)` T5 定义即用。`pickProactive/shouldFire` T13 定义即用。
- **占位策略**：sprite sheet / 表情 / 音效全部「占位脚本先行 + 真资源换文件即生效」，和 v1 一致，不阻塞执行。
- **无 placeholder 红旗**：每个 code step 都有完整代码；asset 任务有具体脚本 + 来源说明；无 "TBD/TODO/similar to Task N"。
